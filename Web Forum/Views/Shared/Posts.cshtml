@model List<Web_Forum.Models.PostViewModel>

<div>
    @foreach (var post in Model.OrderBy(x => x.Posted))
    {
        <div id="postThreads" class="panel panel-default">
            <div class="panel-heading">
                <h4>@post.Name - @post.Posted</h4>
            </div>
            <div class="panel-body">
                <p>@post.Text</p>
            </div>
            

        </div>
    }
</div>